// GitHub Pages uses an explicitly configured API. The enterprise server
// overrides this file with same-origin configuration when it serves the UI.
window.MIYAR_CONFIG = window.MIYAR_CONFIG || {apiBase: ''};
