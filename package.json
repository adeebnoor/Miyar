{
  "name": "miyar-demo",
  "version": "4.3.0",
  "private": true,
  "description": "Bilingual workforce architecture with versioned references and a tenant-scoped enterprise API",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "jsdom": "26.1.0"
  }
}
